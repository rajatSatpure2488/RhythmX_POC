# patient_uniqueness.py
# Checks whether the patient already exists in DrChrono EHR
# Search strategy: first_name + last_name + date_of_birth
# Returns: PatientCheckResult(exists: bool, patient_id: Optional[str])
# If exists → UPDATE flow; if not → CREATE flow

# TODO: implement
