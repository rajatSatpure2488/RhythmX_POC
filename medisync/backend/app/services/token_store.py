# token_store.py
# Secure in-session token management
# Stores: access_token, refresh_token, doctor_id, expiry_time
# Current scope: in-memory (single session)
# Future: extend to encrypted DB persistence for multi-user support
# Methods: set_token(), get_token(), clear_token(), is_token_valid()

# TODO: implement
