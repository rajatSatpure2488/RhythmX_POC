// AuthPanel.jsx
// Phase 1 — EHR Authentication
// Handles OAuth flow (DrChrono redirect) + Manual Token input fallback
// Displays connection status, access token, and doctor ID after auth

export default function AuthPanel() {
  return null; // TODO: implement
}
